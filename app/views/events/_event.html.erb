<div class="content margin8">
  <h2><%= event.description %></h2>
  <div class="text_red"><%= event.date %></div>
  <div class="capitalise italic">Creator: <%= User.find(event.creator_id).name %></div>
  <div><%= link_to 'Show', event, class: "links margin8" %></div>
</div>

<p>
  <strong>Name:</strong>
  <span class="page_title"><%= @user.name%></span>
</p>

<p>
  <strong class="heading">Events Created:</strong>
  <% @user.events.each do |event| %>
    <p><%= event.description %></p>
  <% end %>
</p>

<%
=begin%>
 <p>
  <strong>All Events Attended:</strong>
  <% @user.attended_events.each do |event| %>
    <p><%= event.description %></p>
  <% end %>
</p>
<%
=end%>

<p class="heading">
  <strong>Previously attended Events:</strong>
  <% @user.attended_events.past_events.each do |event| %>
    <p><%= event.description %> <%= event.date %></p>
  <% end %>
</p>

<p class="heading">
  <strong>Upcoming Events to attend:</strong>
  <% @user.attended_events.upcoming_events.each do |event| %>
    <p><%= event.description %> <%= event.date %></p>
  <% end %>
</p>
